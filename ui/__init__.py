"""UI adapters for different front-ends (CLI/Web/TUI).

This package provides a thin abstraction used by actions to request
input and emit events without binding directly to stdin/stdout or
specific frameworks.
"""

