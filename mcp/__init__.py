"""Minimal MCP scaffolding.

This package provides opt-in client stubs so we can wire MCP later without
pulling heavy deps or changing default behavior. Real protocol wiring can be
added behind the `[MCP] active=true` flag and optional extras.
"""

