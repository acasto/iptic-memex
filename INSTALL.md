# Installation

Installation and configuration docs have moved to `docs/getting-started.md`.

Quick start:

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

See: docs/getting-started.md
